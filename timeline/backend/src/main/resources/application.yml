spring:
  datasource:
    url: jdbc:mysql://localhost:3306/timeline_todo?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 1123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          time_zone: Asia/Shanghai
server:
  port: 8080
